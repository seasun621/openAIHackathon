# SWING//BEAT 프로토타입 개발 계획

> 이 문서는 구현 기준, 새로 내린 설계 결정, 진행률과 플레이 테스트 결과를 한곳에서 관리한다.
> 마지막 갱신: 2026-08-20

## 1. 한 문장 정의

`osu!`의 조준·타이밍 판정을 1인칭 3D 자동주행 공간으로 옮기고, 기존 `SUPER SWING`의 양손 사격과 웹스윙을 각각 탭·트레이스·롱 노트로 재해석한 리듬 액션 게임.

## 2. 이번 프로토타입의 성공 조건

- 약 1분 동안 미리 제작된 도시 코스를 자동으로 주행한다.
- 화면 안이 아니라 실제 3D 공간에 있는 노트가 플레이어에게 다가오는 느낌이 난다.
- 단발 노트, 레이저 트레이스, 웹스윙 롱 노트가 서로 다른 손맛을 낸다.
- 웹스윙 유지 중 반대 손으로 단발 노트를 처리할 수 있다.
- 모든 노트에 시간 판정이 있고 콤보, 정확도, 점수로 결과를 확인할 수 있다.
- 실제 음원과 동기화된 약 1분 채보에서 효과음, 노트와 코스 연출을 함께 체험할 수 있다.

## 3. 확정한 설계 원칙

### 시간과 이동을 분리한다

노트 판정은 고정된 리듬 타임라인을 기준으로 한다. 플레이어의 기본 위치는 미리 만든 코스를 따라 자동 이동하며, 스윙으로 발생한 물리 이동은 이 기본 경로 위에 오프셋으로 더한다. 이 구조를 사용하면 스윙의 운동감은 남기면서도 플레이어마다 노트 도착 시간이 달라지는 리듬게임의 불공정성을 막을 수 있다.

### 노트는 월드 오브젝트다

노트는 카메라에 붙는 2D UI가 아니라 도시 안에 배치한다. 다만 접근 링, 판정 텍스트, 콤보처럼 읽기 위한 정보는 화면 UI를 함께 사용한다. 목표 시각에는 노트가 카메라 정면 약 14m 앞의 편안한 사격 지점에 도달하도록 배치한다.

### 경로는 고정, 도시 외형은 가볍게

도로의 직선과 커브는 사전에 고정한다. 건물 높이와 조명 배치는 고정 시드를 이용해 반복 제작 비용을 줄이되, 플레이 경로 자체는 절차적으로 바뀌지 않는다.

## 4. 조작과 노트 규칙

| 입력 | 역할 |
| --- | --- |
| 마우스 이동 | 조준 |
| 오른쪽 버튼 클릭 | 단발 노트 사격 |
| 오른쪽 버튼 유지/해제 | 레이저 노트 시작·추적·종료 |
| 왼쪽 버튼 유지/해제 | 웹스윙 롱 노트 연결·유지·해제 |
| `R` | 현재 차트 처음부터 재시작 |
| `Esc` | 마우스 잠금 해제 |

### 단발 노트

- 중앙 조준선으로 노트를 맞춘다.
- 클릭 시각과 노트 기준 시각의 차이를 판정한다.
- 노트가 판정 구간을 지나면 `MISS` 처리한다.

### 레이저 트레이스 노트

- 시작점을 조준해 오른쪽 버튼을 누른다.
- 버튼을 유지한 채 마우스를 직접 움직여 슬라이더 볼을 중앙 `DRAG` 원 안에 둔다.
- 볼이 `DRAG` 원을 벗어나면 즉시 실패하며, 실제 이동 방향과 거리도 최종 판정에 사용한다.
- 일반 슬라이더는 끝에서 자동 종료되고 시작 타이밍, 추적 성공 비율, 유효 드래그 거리를 합산한다.

### 웹스윙 롱 노트

- 청록색 앵커를 조준해 왼쪽 버튼을 누른다.
- 유지 중에는 카메라가 자동주행 경로 위에서 스프링·감쇠 기반의 원호 오프셋을 만든다.
- 연결 타이밍, 유지 비율, 해제 타이밍을 합산한다.
- 웹 유지 중에도 오른쪽 버튼 입력과 조준은 막지 않는다.

### 임시 판정 폭

| 판정 | 절대 시간 오차 |
| --- | --- |
| PERFECT | 55ms 이하 |
| GREAT | 105ms 이하 |
| GOOD | 180ms 이하 |
| BAD | 280ms 이하 |
| MISS | 280ms 초과 또는 입력 없음 |

롱 노트는 시작 정확도 35%, 유지율 40%, 해제 정확도 25%를 합산해 최종 판정을 낸다. 수치는 첫 플레이 테스트 이후 조정한다.

## 5. 1분 체험 차트 구성

| 구간 | 목적 |
| --- | --- |
| 0–10초 | 느린 좌우 단발 노트로 조준과 접근 링 학습 |
| 10–20초 | 짧은 연속 사격과 첫 레이저 트레이스 |
| 20–31초 | 첫 웹스윙 롱 노트와 해제 타이밍 학습 |
| 31–43초 | 레이저 변형과 커브 구간 |
| 43–53초 | 웹스윙 중 단발 노트를 처리하는 복합 패턴 |
| 53–60초 | 단발·레이저·스윙을 다시 섞는 피날레 |

## 6. 개발 단계와 현재 진척

- [x] 기존 저장소와 재사용 가능한 스윙·사격·3D 렌더링 구조 확인
- [x] 게임 루프, 노트 분류, 판정 원칙과 임시 수치 확정
- [x] 고정 코스 자동주행과 도시 환경 구현
- [x] 리듬 시계와 1분 차트 데이터 구현
- [x] 단발 노트와 접근 링, 폭발 판정 구현
- [x] 레이저 트레이스 노트 구현
- [x] 웹스윙 롱 노트와 이동 오프셋 구현
- [x] 동시 노트, 점수, 콤보, 정확도 UI 구현
- [x] 임시 박자음과 상호작용 효과음 구현
- [x] 빌드 검사와 브라우저 플레이 테스트
- [x] 테스트 결과를 반영한 수치·가독성 1차 조정

현재 단계: **9차 실제 음원 재생·140 BPM 60초 채보·오디오 마스터 시계 연결 완료 — 플레이 감각 검증 대기**

## 7. 첫 플레이 테스트에서 확인할 질문

1. 노트가 화면에 붙어 있는 것이 아니라 공간 속에서 다가오는 것으로 느껴지는가?
2. 마우스를 크게 돌리지 않고도 다음 노트를 발견할 수 있는가?
3. 접근 링만 보고 임시 박자를 예측할 수 있는가?
4. 레이저의 현재 추적 지점과 남은 경로가 명확히 구분되는가?
5. 스윙 진입·유지·해제 시점이 시각적으로 읽히는가?
6. 스윙과 사격 동시 수행이 어렵지만 이해 가능한 수준인가?
7. 커브 구간에서 카메라의 자동 회전이 멀미를 유발하지 않는가?

## 8. 이번 단계에서 제외하는 것

- 최종 음악과 실제 음원 기반 채보
- 채보 에디터와 곡 선택 화면
- 캐릭터, 완성형 총기, 고품질 건물 에셋
- 모바일·게임패드 입력
- 랭킹, 저장, 온라인 기능
- 최종 UI 디자인과 접근성 옵션 전체

## 9. 결정 로그

- **2026-08-20:** 자유 이동형 물리 샌드박스 대신 고정 시간축 자동주행을 채택했다.
- **2026-08-20:** 오른손은 단발/레이저, 왼손은 웹스윙으로 역할을 고정했다.
- **2026-08-20:** 음악이 없는 프로토타입은 120 BPM 내부 박자와 합성 효과음을 사용한다.
- **2026-08-20:** 3D 월드 노트와 2D 가독성 보조 UI를 혼합한다.
- **2026-08-20:** 리듬 판정의 재현성을 위해 자유 낙하 강체 대신 스프링·감쇠 기반의 제한된 스윙 오프셋을 사용한다. 향후 재미가 검증되면 경로를 유지하는 범위 안에서 로프 제약을 강화한다.
- **2026-08-20:** 차트 시간은 프레임 누적값이 아니라 고정 시작 시각과 현재 시각의 차이로 계산한다. 낮은 프레임률에서도 노트 판정 시간이 늘어지지 않는다.

## 10. 진행 기록

- **2026-08-20 / 계획 수립:** 기획안을 구현 가능한 시스템 단위로 분해하고 판정 및 코스 원칙을 확정했다.
- **2026-08-20 / 기반 구현:** 고정 코스, 60초 차트, 자동주행 카메라와 도시 협곡을 구현했다.
- **2026-08-20 / 노트 구현:** 단발 사격, 3D 레이저 트레이스, 웹스윙 롱 노트와 복합 패턴을 구현했다.
- **2026-08-20 / 피드백 구현:** 5단계 판정, 콤보, 정확도, 점수, 결과 화면과 합성 효과음을 추가했다.
- **2026-08-20 / 1차 검증:** 정적 빌드를 통과했다. 브라우저에서 시작 화면, 자동주행, 단발 접근 링, 레이저 궤도와 웹 앵커를 구간별로 확인했고 콘솔 오류가 없음을 확인했다.
- **2026-08-20 / 판정 시계 보정:** 낮은 프레임률에서 누적 시간이 느려지는 현상을 확인해 프레임 독립적인 고정 시각 차트 시계로 교체했다.

## 11. 현재 알려진 한계와 다음 조정 후보

- 음악이 없으므로 현재 차트는 박자음과 시각 접근 링만으로 체감한다.
- 첫 플레이에서 노트를 놓치지 않으려면 좌우 에임 범위와 노트 크기를 실제 사용자 플레이로 조정해야 한다.
- 웹스윙은 리듬 안정성을 우선한 제한형 물리다. 이후 스윙 재미가 부족하면 로프 장력, 속도감, 카메라 롤을 단계적으로 추가한다.
- 도시 건물은 임시 도형이며, 도로 구간과 노트 가독성 검증 후 아트 방향을 결정한다.

## 12. 사용자 피드백 반영 — 2차 개선

### 반영한 문제

- [x] 기본 주행 속도가 느리고 주변 통과 정보가 부족한 문제
- [x] 도로와 건물의 명도가 낮아 공간 구조가 검게 뭉치는 문제
- [x] 접근 링 외에는 판정 순간을 알려주는 신호가 부족한 문제
- [x] 박자음·사격음이 작고 명중 반동과 폭발이 약한 문제

### 적용 내용

- 기본 주행 속도를 `12m/s`에서 `22m/s`로 올렸다.
- 기본 시야각을 72도에서 80도로 넓히고, 사격과 스윙 시 짧은 시야각 펀치를 추가했다.
- 도로 표식 간격을 줄이고 양쪽 가장자리에 청록·분홍 발광 스트립을 배치했다.
- 건물 재질, 하늘, 안개와 전체 조명을 밝히고 건물 면에 자체 미광을 추가했다.
- 노트의 시간 차이를 `기본색 → 노랑 준비 → 초록 판정 가능 → 흰색 PERFECT → 주황 지각`으로 표시한다.
- 노트 본체, 외곽 와이어 셸, 접근 링, 주변 광원과 중앙 `FIRE` 신호가 같은 판정 색을 사용한다.
- 레이저와 웹스윙은 시작뿐 아니라 해제 시점에도 준비음과 색상 변화를 반복한다.
- 사전 신호음을 노트의 좌우 위치에 맞춰 스테레오로 재생한다.
- 박자음, 사격음, 웹 연결음과 판정음을 여러 음역의 레이어로 보강했다.
- 명중 등급에 따라 무기 반동, 카메라 충격, 시야각 펀치, 화면 섬광과 파편량이 달라진다.

### 2차 브라우저 점검

- 밝아진 도시 면과 도로 구분을 확인했다.
- 단발, 레이저, 웹 앵커에서 노랑·초록·흰색 판정 단계가 모두 표시되는 것을 확인했다.
- 연속 노트 중에도 중앙 판정 신호가 다음 입력 시점을 갱신하는 것을 확인했다.
- 개발자 콘솔 오류와 경고가 없음을 확인했다.

다음 플레이 테스트에서는 속도가 과하지 않은지, 중앙 `FIRE` 신호가 노트를 가리지 않는지, 합성 효과음의 음량 균형을 실제 청취로 조정한다.

## 13. 사용자 피드백 반영 — 3차 공중 비행 개선

### 반영한 문제

- [x] 도로 위를 걷는 듯한 이동을 빠른 제트 비행 감각으로 교체
- [x] 같은 높이와 좁은 길목에 몰리던 노트를 공중 경로 전체로 분산
- [x] 정밀 히트스캔에 가까웠던 조준 판정을 리듬 중심의 보조 판정으로 완화
- [x] 웹스윙을 직선 이동뿐 아니라 사전 제작된 코너 연출과 결합

### 적용 내용

- 기본 속도 기준을 `22m/s`에서 `36m/s`로 높이고 기본 시야각을 88도로 확장했다.
- 직선 도로 좌표 대신 고도 상승·하강과 네 번의 큰 방향 전환이 포함된 공중 `Catmull-Rom` 비행 경로를 사용한다.
- 카메라가 경로 경사를 따라 들리고 숙여지며, 코너 진입 시 회전 방향으로 자동 뱅킹한다.
- 웹스윙 구간을 주요 코너 시점에 배치하고, 성공 시 횡이동·상승·롤을 크게 더해 건물 사이를 원호로 휘어 도는 연출을 만든다.
- 화면 가장자리 집중선과 카메라 내부 공간 속도선을 겹쳐 중앙 노트의 가독성을 유지하면서 고속감을 강화했다.
- 낮은 제트 엔진음을 플레이 중 지속 재생하고 시작·종료 시 자연스럽게 페이드한다.
- 단발 노트를 좌우 최대 약 6.8m, 상하 최대 약 5.5m에 배치하고 레이저 궤도에도 상하 파동을 추가했다.
- 웹 앵커는 상단 좌우에 놓되 화면 밖으로 벗어나지 않도록 위치를 제한했다.
- 실제 보이는 노트보다 큰 투명 구체를 판정 영역으로 사용한다. 단발·레이저·웹 앵커 순으로 허용 반경을 넓혀 정확한 한 점보다 대략적인 영역 조준을 우선한다.

### 3차 브라우저 점검

- 공중 경로에서 도로가 아래로 멀어지고 건물 사이 고도와 방향이 바뀌는 것을 확인했다.
- 코너 구간에서 건물과 도로가 함께 기울어 카메라 뱅킹이 읽히는 것을 확인했다.
- 화면 가장자리와 3D 공간의 속도선이 중앙 노트와 판정 링을 가리지 않는 것을 확인했다.
- 상하·좌우로 분산된 단발 노트와 레이저 경로가 화면 안에서 구분되는 것을 확인했다.
- 갱신 후 정적 빌드를 다시 통과했다.

다음 플레이 테스트에서는 `36m/s`의 체감 속도, 코너 스윙의 원호 크기, 보이지 않는 조준 보조 반경이 과하거나 부족하지 않은지 실제 입력으로 미세 조정한다.

## 14. 사용자 피드백 반영 — 4차 속도 연출 개선

### 적용 내용

- 중앙을 향해 길게 뻗던 방사형 집중선과 3D 흰색 속도선을 제거했다.
- 상·하·좌·우 네 변의 약 20% 영역에만 청록·분홍 블러 잔상이 머물도록 변경했다.
- 테두리 잔상은 진행 방향과 수직인 짧은 선이 늘어났다 줄어들며 중앙 시야는 완전히 비워 둔다.
- 비행 속도 기준을 `36m/s`에서 `48m/s`로 높이고, 60초 코스의 실제 월드 길이도 약 30% 확장했다.
- 기본 시야각을 92도로 넓히고 스윙·명중 시 시야각 펀치를 강화했다.
- 상시 제트 진동, 무기 흔들림, 명중 카메라 충격의 세기와 지속 시간을 높였다.
- 코너 자동 뱅킹 최대치를 높이고 웹스윙 중 추가 롤을 강화했다.
- 제트 엔진음의 음높이와 음량을 올리고, 명중 화면 섬광의 밝기와 가장자리 번짐을 키웠다.

### 4차 브라우저 점검

- 네 변의 블러 집중선이 중앙 조준점과 접근 노트를 침범하지 않는 것을 확인했다.
- 초반 직선 구간에서 기존의 공간 속도선 없이도 테두리 잔상과 빠른 건물 통과로 속도감이 유지되는 것을 확인했다.
- 중반 코너에서 넓어진 카메라 뱅킹과 고도 변화가 화면에 명확히 읽히는 것을 확인했다.
- 갱신 후 정적 빌드를 통과했다.

다음 플레이 테스트에서는 테두리 블러의 강도와 `48m/s` 속도에서 노트 사전 인지 시간이 충분한지 실제 입력으로 조정한다.

## 15. 사용자 피드백 반영 — 5차 일반 롱노트 재설계

### 역할 분리

- 웹스윙은 2.5~3.5초 동안 진입·유지·해제를 평가하는 긴 롱노트 역할을 유지한다.
- 일반 롱노트는 1.2~1.4초 동안 빠르게 마우스를 움직이는 `드래그 슬라이더`로 변경한다.
- 일반 슬라이더는 끝에서 자동 종료되며 정확한 해제 타이밍을 평가하지 않는다.

### osu!-형 시각 언어

- 분홍색 캡슐 경로 바깥에 굵은 흰 외곽선을 배치했다.
- 경로 양 끝에 흰 테두리가 있는 시작·종료 원을 추가했다.
- 현재 추적 지점에 분홍 슬라이더 볼과 큰 팔로우 링을 표시한다.
- 슬라이더를 잡으면 중앙 입력 UI가 `FIRE`에서 `DRAG`로 바뀌고 분홍색 팔로우 원으로 맥동한다.
- 안내 문구와 범례를 `HOLD & TRACE`에서 `DRAG SLIDER`로 변경했다.

### 판정 변경

- 일반 슬라이더의 보이지 않는 조준 반경을 `2.6m`에서 `1.75m`로 줄였다.
- 시작 타이밍은 최종 점수의 20%, 실제 궤도 추적률은 80%를 차지한다.
- 누르고만 있고 움직이는 슬라이더 볼을 따라가지 않으면 `MISS`가 되도록 변경했다.
- 짧아진 시간에 맞춰 상하 파동 폭은 낮춰 빠르지만 읽을 수 있는 마우스 동선을 유지했다.

### 음향과 검증

- 제트 엔진음을 거친 톱니파에서 부드러운 삼각파로 바꾸고 음량을 `0.05`에서 `0.014`로 낮췄다.
- 브라우저에서 첫 슬라이더의 분홍 캡슐 경로, 흰 외곽선, 양 끝 원이 도시 배경 위에서 구분되는 것을 확인했다.
- 갱신 후 정적 빌드를 통과했다.

다음 플레이 테스트에서는 1.2~1.4초 추적 시간이 너무 짧지 않은지와 `1.75m` 팔로우 판정 반경을 실제 마우스 입력으로 조정한다.

## 16. 사용자 피드백 반영 — 6차 슬라이더 실루엣 개선

### 원형 노트 확대

- 단발 노트 본체 반지름을 `0.92m`에서 `1.08m`로 키웠다.
- 단발 노트의 흰 안쪽 원, 접근 원과 와이어 셸도 같은 비율로 확대했다.
- 슬라이더 시작·종료 원 반지름을 `1.1m`에서 `1.5m`로 키우고 흰 외곽선을 `1.78m`까지 확장했다.

### 3D osu!-형 캡슐 슬라이더

- 흰 외피 반지름을 `0.72m`에서 `1.08m`로, 분홍 본체를 `0.5m`에서 `0.82m`로 확장했다.
- 경로의 단면 해상도를 높여 가까이에서도 둥근 3D 캡슐로 보이게 했다.
- 이동 지점에 붙어 경로를 따라가던 3D 팔로우 링을 제거했다.
- 대신 흰 외곽 실루엣과 분홍 내부를 가진 채워진 3D 슬라이더 볼이 경로 위를 이동한다.
- 슬라이더 시작 전에는 접근 원을 보여주고, 시작 후에는 접근 원과 와이어 셸을 숨겨 볼과 캡슐 몸통에만 집중하게 했다.
- 커서 중심의 `DRAG` 추적 UI는 조작 범위를 알려주는 역할로 유지한다.

### 6차 브라우저 점검

- 첫 슬라이더 타격 직전 구간에서 흰 외피와 분홍 본체의 두께 차이가 명확히 보이는 것을 확인했다.
- 확대된 시작 원이 캡슐 몸통과 하나의 osu! 슬라이더 실루엣으로 연결되는 것을 확인했다.
- 중앙 노트와 도심 배경 위에서도 슬라이더 경로가 묻히지 않는 것을 확인했다.
- 갱신 후 정적 빌드를 통과했다.

다음 플레이 테스트에서는 확대된 원이 연속 단발 패턴에서 서로 과하게 겹치지 않는지와 캡슐 두께가 원근상 적절한지 조정한다.

## 17. 사용자 피드백 반영 — 7차 실제 드래그 판정

### 확인한 원인

- 기존 판정은 오른쪽 버튼 유지 시간과 슬라이더 볼 조준 여부만 누적했다.
- 슬라이더가 중앙 조준점 근처를 통과하면 마우스 이동량이 0이어도 추적률이 올라갈 수 있었다.

### 변경한 판정

- 매 렌더 프레임 사이에 발생한 실제 마우스 `movementX`, `movementY`를 누적한다.
- 현재 슬라이더 볼과 75ms 뒤의 볼 위치를 화면 좌표로 투영해 다음 진행 방향을 계산한다.
- 마우스 이동 방향과 슬라이더 진행 방향의 내적을 이용해 유효 방향 이동량만 기록한다.
- 버튼 유지·조준 추적은 55%, 시작 타이밍은 15%, 유효 방향 드래그 거리는 30%를 차지한다.
- 슬라이더별 요구 드래그 거리는 약 168~176px이며 진행 방향과 맞는 이동만 인정한다.
- 유효 방향 드래그가 요구량의 18% 미만이면 다른 조건과 무관하게 최종 품질을 0으로 고정한다. 따라서 가만히 누르고 있는 입력은 반드시 `MISS`다.
- 조준, 버튼 유지, 실제 이동과 방향이 동시에 맞는 프레임만 슬라이더 볼과 `DRAG` UI가 초록색으로 반응한다.

### 검증

- 타입 검사와 정적 빌드를 통과했다.
- 정지 입력의 유효 방향 드래그 값은 0이며 명시적인 최소 이동량 분기에서 `MISS`로 종료되는 것을 확인했다.

다음 플레이 테스트에서는 손목으로 자연스럽게 따라갔을 때 필요한 168~176px 요구량과 방향 허용 각도가 너무 엄격하지 않은지 조정한다.

## 18. 사용자 피드백 반영 — 8차 중앙 팔로우 판정과 실제 음원 채보 계획

### 슬라이더 자동 추적 제거

- 오른손 레이저가 움직이는 슬라이더 볼을 자동으로 향하던 시각 연출을 제거했다. 이제 레이저는 항상 화면 중앙 에임 방향으로 직진한다.
- 슬라이더 볼의 3D 위치를 매 프레임 화면 좌표로 투영하고, 화면 중앙 `DRAG` 원의 반지름인 약 `58 CSS px` 안에 있는지 직접 검사한다.
- 슬라이더를 잡은 뒤 볼이 중앙 원을 `60ms` 이상 벗어나면 해당 슬라이더를 즉시 `MISS`로 확정하고 본체와 레이저를 끊는다. 한 프레임 흔들림만 흡수하고 체감상 바로 실패하도록 잡은 값이다.
- 기존의 실제 마우스 이동량·진행 방향·최소 드래그 거리 조건도 그대로 유지한다. 따라서 경로가 우연히 중앙을 지나는 동안 가만히 있어도 성공할 수 없다.

### 임시 음원 분석 결과

대상 파일: `music/alex-morgan-heavy-dubstep-bass-drop-edm-530942.mp3`

| 항목 | 분석값 |
| --- | --- |
| 재생 길이 | `156.000초` (`2:36`) |
| 원본 포맷 | MP3, `48kHz`, 스테레오 |
| 기준 템포 | `140 BPM` |
| 1박 길이 | `0.428571초` |
| 첫 비트 후보 | 약 `0.058초` |
| 강한 에너지 구간 | 약 `41~109초`, `123.5~150.9초` |
| 브레이크 후보 | 약 `109.8~123.5초` |
| 최고 밀도 60초 후보 | 약 `43.5~103.5초` |

- 저해상도 단일 템포 검출에서는 `143.55 BPM` 빈도도 나왔지만, 더 촘촘한 분석과 전체 비트 시각의 선형 회귀값은 각각 `139.67 BPM`, `140.00 BPM`으로 수렴했다. 따라서 채보 기준은 `140 BPM`으로 확정한다.
- 첫 비트 `0.058초`는 자동 검출값이므로 실제 게임에 음악을 연결한 뒤 메트로놈을 겹쳐 ±10~20ms 범위에서 귀로 최종 보정한다.
- 에너지와 타격 밀도를 기준으로 보면 `0~13.7초`는 도입, `13.7~41초`는 상승, `41~109초`는 첫 고강도 구간, `109.8~123.5초`는 브레이크, `123.5~150.9초`는 두 번째 고강도 구간, 이후는 아웃트로에 가깝다.

### 1분 프로토타입 채보 범위

- 본 채보는 `96.000~156.000초` 구간을 사용한다. 첫 비트는 게임 시간 `0.058초`에 두어 140 BPM 격자를 유지하면서 정확히 60초를 재생한다. 첫 고강도 구간의 말미, 브레이크, 재상승, 두 번째 드롭과 아웃트로가 한 번에 들어와 세 노트 타입과 코스 변화를 모두 시험하기 좋다.
- 순수 노트 밀도 스트레스 테스트가 필요하면 별도 테스트 범위로 자동 점수가 가장 높았던 `43.5~103.5초`를 사용한다.
- 게임 시간 0초는 음원 `96.000초`에 대응시키고, 첫 박 오프셋 `0.058초`를 포함한 모든 노트 시각을 140 BPM 박 단위로 계산한다.

### 박자 단위별 노트 배분

- **단발 노트:** 킥·스네어와 강한 트랜지언트를 우선 사용한다. 기본은 1/2박(`214ms`) 이상 간격을 두고, 드롭의 짧은 강조에만 1/4박(`107ms`) 두 발 패턴을 제한적으로 사용한다.
- **일반 드래그 슬라이더:** 길게 이어지는 베이스 움직임이나 3박짜리 프레이즈에 배치한다. `3박 = 1.286초`를 기본 길이로 삼아 현재 1.2~1.4초 조작 설계와 정확히 맞춘다.
- **웹스윙:** 6박(`2.571초`) 또는 8박(`3.429초`)으로 만든다. 브레이크 진입·드롭 직전·큰 코너처럼 코스 연출의 전환점에만 사용한다.
- **동시 노트:** 웹스윙 유지 중 반대 손 단발 노트는 스네어·강한 보조 타격에만 배치한다. 초벌 채보에서는 한 스윙당 2~4개로 제한한다.
- **휴식 구간:** 브레이크에서는 밀도를 40~55%까지 낮추고, 시선 이동이 큰 슬라이더 하나 또는 웹스윙 하나를 중심으로 구성해 다음 드롭을 준비하게 한다.

### 음악과 코스 동기화 계획

1. 음악의 `AudioContext.currentTime`을 게임의 마스터 시계로 바꾸고, 렌더 프레임 누적 시간은 화면 보간에만 사용한다.
2. 차트에 `audioFile`, `bpm`, `beatOffset`, `clipStart`, `inputOffset` 메타데이터를 추가한다.
3. 자동 검출한 강한 어택을 140 BPM 격자에 스냅해 단발 노트 초안을 만들되, 한 박 안의 모든 피크를 그대로 노트로 만들지는 않는다.
4. `109.8초`, `123.5초`, `150.9초` 전후의 큰 구간 변화를 코스의 고도 변화·코너·웹스윙 진입과 맞춘다.
5. 베이스 지속 구간은 3박 슬라이더 후보, 6~8박 프레이즈는 웹스윙 후보로 분류한 뒤 손으로 동선과 겹침을 편집한다.
6. 실제 플레이에서 입력 오프셋을 조정하고, 1분 끝까지 음악과 노트의 누적 드리프트가 `20ms` 이내인지 확인한다.

### 다음 구현 순서

- [x] 음원 재생기와 재시작·종료 수명주기를 게임 상태에 연결
- [ ] 140 BPM/0.058초 기준 메트로놈 오버레이로 첫 박 수동 검증
- [x] `96.000~156초` 구간의 단발 노트 초벌 생성
- [x] 3박 슬라이더와 6·8박 웹스윙을 프레이즈 및 코너에 수동 배치
- [x] 동시 노트와 휴식 구간을 포함한 1차 난이도 곡선 구성
- [ ] 효과음·엔진음이 음악을 가리지 않도록 최종 믹스 재조정

## 19. 실제 음원 플레이 구현 — 9차 음악 채보

### 오디오가 게임의 기준 시계

- MP3 전체를 시작 시 Web Audio `AudioBuffer`로 디코딩하고 음원 `96.000초`부터 정확히 60초만 재생한다.
- 음악 소스를 예약 재생한 `AudioContext.currentTime`을 게임 시간 0초로 삼는다. 자동 비행, 노트 판정, 접근 연출, 남은 시간 UI가 모두 이 값을 사용한다.
- 브라우저 렌더링이 잠깐 느려져도 프레임 누적 오차가 음악과 채보 사이에 쌓이지 않는다.
- 재시작 시 기존 음악 소스와 지속 효과음을 즉시 끊고 같은 구간을 처음부터 다시 예약한다. 결과 화면에서는 음악·레이저·제트음을 모두 정지한다.
- 임시 120 BPM 메트로놈은 제거했다. 노트 사전 신호와 판정·무기 효과음만 음악 위에 남긴다.

### 실제 140 BPM 채보

- 곡의 첫 박 후보 `0.058초`와 `140 BPM`을 이용해 모든 노트 시간을 박 번호로 계산한다.
- 총 `58개` 노트(단발 48, 슬라이더 6, 웹스윙 4)를 배치했다. 일반 슬라이더는 모두 3박(`1.286초`), 웹스윙은 6박(`2.571초`) 또는 8박(`3.429초`) 길이다.
- 게임 시간 `13.8~18.9초`의 저밀도 브레이크에는 웹스윙 하나만 두어 휴식을 만들었다.
- `18.9~20.6초`의 갑작스러운 강한 타격 다섯 개와 `25.8~32.2초`의 빌드업은 분석된 트랜지언트 위치를 직접 사용했다.
- `35.2~39.5초`의 최고 강도 구간은 한 박 간격 연속 조준으로 밀도를 가장 높였다.
- 웹스윙 3개 구간에는 오른손 단발 노트를 함께 넣어 양손 동시 처리 기믹을 확인할 수 있다.
- 마지막 강한 타격은 약 `56.2초`에 끝내고 남은 약 3초는 아웃트로 비행과 결과 전환을 위한 여백으로 남겼다.

### 화면과 로딩 처리

- 시작 화면과 상단 HUD에 실제 곡명, 140 BPM, `MUSIC CHART 01`을 표시한다.
- 첫 실행에서 5MB MP3를 디코딩하는 동안 `TRACK SYNC` 로딩 화면을 유지하고 중복 시작 입력을 막는다.
- 음원 로딩이나 디코딩이 실패하면 원인을 시작 화면에 표시하고 다시 시도할 수 있다.

### 다음 플레이 테스트 점검값

- [ ] 첫 킥과 게임 시간 `0.058초` 격자가 귀로 일치하는지 확인
- [ ] 음악 대비 노트 사전 신호·사격·판정음·제트음의 음량 균형 조정
- [ ] 최고 밀도 구간의 한 박 연타가 3D 조준 이동 거리와 함께 처리 가능한지 확인
- [ ] 저밀도 브레이크의 웹스윙이 음악의 공간감과 코너 연출에 맞는지 확인
- [ ] 재시작을 여러 번 반복해도 음악이 겹치거나 채보 시간이 어긋나지 않는지 확인

### 9차 브라우저 검증

- 시작 화면에서 `140 BPM // MUSIC CHART 01`, 곡명, `1:36–2:36` 재생 구간이 표시되는 것을 확인했다.
- 시작 버튼을 누른 뒤 MP3 로딩과 디코딩이 성공해야만 오버레이가 닫히고 `140 BPM // MUSIC SYNC` 상태로 진입하는 것을 확인했다.
- 실행 약 2초 뒤 남은 시간이 `0:58`로 진행되어 오디오 기준 게임 시계가 동작하는 것을 확인했다.
- 실행 중 `R`을 눌렀을 때 남은 시간이 `0:59`로 돌아가고 새 음원 소스와 채보가 함께 다시 시작되는 것을 확인했다.
- 첫 단발 패턴과 3박 슬라이더가 새 채보 시각에 맞춰 도시 공간에 나타나는 것을 확인했다.
- 타입 검사, 정적 빌드와 배포용 MP3 에셋 생성을 통과했다.
